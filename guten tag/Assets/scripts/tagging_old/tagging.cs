using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tagging : MonoBehaviour {
/*
	//public GameObject otherGameObject;
	public bool istag = false;
	//tagging tagginge;
	public int number = 0;
	public bool istagstore;
	public float timer2 = 3f;
	public float timer;
	float othertimer;
	// Use this for initialization
	void Start () {
		//tagginge = otherGameObject.GetComponent<tagging> ();
		istagstore = istag;
		timer = timer2;
	}
	
	// Update is called once per frame
	void Update () {
		if (timer > 0) {
			timer -= Time.deltaTime;
			//ticks timer if timer has value
			//print (timer);
		}

	}
		void OnTriggerStay2D (Collider2D other)
	{
		GameObject gObj = other.gameObject;
		// get collided obj
		tagginge = other.GetComponent<tagging> ();
		// get other tag script
		bool istagged = tagginge.istagstore;
		istagstore = istag;
		othertimer = tagginge.timer;
		//Debug.Log ("istagged " + istagged + " num " + number + " wow");
	


		if (gObj.CompareTag ("Tagging") && timer <= 0 && othertimer <= 0) {
			if (istagged == true && istag == false) {
				Debug.Log ("istagged " + istagged + " istagstore " + istagstore + " num " + number + " t");
				istag = true;
				Debug.Log ("tagged");
				//timer = timer2;
			}
			if (istagged == false && istag == true) {
				Debug.Log ("istagged " + istagged + " istagstore " + istagstore + " num " + number + " u");
				istag = false;
				Debug.Log ("untagged");
				timer = timer2;
			}
		}

		}
*/
}
